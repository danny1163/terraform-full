# Note: till now, we just declaring the project values here, but now we are only declaring the values here. and values will mention on variable module file
#        will just initialize the variable & will pass the value from outside the vpc directory.

variable "project" {}
variable "vpc_cidr" {}
variable "enviorment" {}
variable "private_subnet_cidr" {}
variable "public_subnet_cidr" {}

